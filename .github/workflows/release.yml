name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Remove BOM from JSON files
        shell: pwsh
        run: |
          Get-ChildItem -Recurse -Include *.json | ForEach-Object {
            $content = Get-Content $_.FullName -Raw
            if ($content -and $content[0] -eq [char]0xFEFF) {
              $content = $content.Substring(1)
              [System.IO.File]::WriteAllText($_.FullName, $content, (New-Object System.Text.UTF8Encoding $false))
              Write-Host "Removed BOM from $($_.FullName)"
            }
          }

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - uses: dtolnay/rust-toolchain@stable

      - name: Install npm dependencies
        run: npm install

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: v__VERSION__
          releaseName: "KaïfBrowser v__VERSION__"
          releaseBody: "Сборка браузера с неоновой красной темой и главным экраном"