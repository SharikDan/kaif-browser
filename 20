          }`}echo           style={activeTabId === tab.id ? { borderBottom: `2px solid ${primary}` } : {}}echo         >echo           <span className="text-sm text-white/80 max-w-[150px] truncate">echo             {tab.title.length  ? tab.title.slice(0, 18) + '.' : tab.title}echo           </span>echo           <buttonecho             onClick={(e) => { e.stopPropagation(); closeTab(tab.id); }}echo             className="opacity-0 group-hover:opacity-100 transition p-1 rounded-full hover:bg-white/20"echo           >echo             <X size={12} className="text-white/70" />echo           </button>echo         </motion.div>echo       ))}echo       <buttonecho         onClick={() => addTab()}echo         className="p-2 rounded-full hover:bg-white/10 transition"echo       >echo         <Plus size={16} className="text-white/80" />echo       </button>echo     </div>;
